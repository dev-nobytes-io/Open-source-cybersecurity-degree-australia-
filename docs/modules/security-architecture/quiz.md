# EXT-SA self-assessment quiz

> **Module type:** Extension series self-assessment — part of [EXT-SA](index.md)
> **Status:** Draft
> **Version:** v0.1
> **Last Reviewed:** 2026-09-12

!!! warning "Not a credit-bearing unit"
    This quiz belongs to the [EXT-SA series](index.md), an optional extension outside the
    168 CP degree structure. It carries **0 CP**, is not assessed, and does not appear in
    `docs/ksat-coverage.md`, which is generated from credit-bearing units only.

**60 questions across the six security architecture modules, ten per module.**
Single-answer questions have one correct option; **multiple-selection** questions
have two or more and are marked as such. Answer a question and press **Check** to
reveal the answer and the reasoning, or use **Check all** at the bottom. The
module chips filter the set; the score counts only the questions in view.

**How to use.** Every question is drawn from the text of one module:
[SA-01](sa-01-business-driven-architecture-in-practice.md),
[SA-02](sa-02-integrating-security-into-enterprise-architecture.md),
[SA-03](sa-03-modern-defensible-architecture-and-csf.md),
[SA-04](sa-04-network-gateway-and-access-architecture.md),
[SA-05](sa-05-logging-and-monitoring-architecture.md) or
[SA-06](sa-06-assurance-maturity-and-authorisation.md). Each explanation names the
module and the topic heading the answer comes from, so a wrong answer points you
at the passage to re-read. Questions mix recall (what a source says), application
(which decision follows from a stated constraint) and judgement (which option the
module argues against, and why). Where a module marks an item provisional or
unverified, the quiz says so rather than testing it as fact; where a question
mentions an ISM identifier, it is the identifier the module cites from the
September 2026 extract, every one of which the modules flag for re-verification,
and no answer turns on an identifier number.

!!! note "Nothing is recorded"
    Your answers stay in the browser and are not submitted anywhere. This is
    formative self-assessment, not the modules' graded work; the assessment
    criteria live in each module page.

!!! warning "Viewing this on GitHub"
    The quiz runs in your browser. On GitHub's raw markdown view the JavaScript
    does not execute, so use the published documentation site.

<style>
.qz{--qz-a:#2e7d32;--qz-b:#c62828;--qz-p:#1565c0;--qz-bd:#d7dce1;--qz-mut:#6b7783;
    --qz-card:rgba(127,127,127,.045);}
.qz-bar{display:flex;flex-wrap:wrap;gap:.4rem;align-items:center;margin:.8rem 0 1rem;}
.qz-chip{font-size:.74rem;padding:.3rem .62rem;border:1px solid var(--qz-p);color:var(--qz-p);
    background:transparent;border-radius:14px;cursor:pointer;font-family:inherit;}
.qz-chip[aria-pressed=true]{background:var(--qz-p);color:#fff;}
.qz-act{font-size:.78rem;padding:.42rem .8rem;border:1px solid var(--qz-p);background:var(--qz-p);
    color:#fff;border-radius:6px;cursor:pointer;font-family:inherit;}
.qz-act.ghost{background:transparent;color:var(--qz-p);}
.qz-score{margin-left:auto;font-size:.82rem;font-weight:700;}
.qz-q{border:1px solid var(--qz-bd);border-radius:9px;padding:.85rem 1rem;margin:.85rem 0;
    background:var(--qz-card);}
.qz-q[hidden]{display:none;}
.qz-meta{display:flex;gap:.5rem;align-items:center;font-size:.7rem;color:var(--qz-mut);
    text-transform:uppercase;letter-spacing:.05em;margin-bottom:.35rem;}
.qz-tag{border:1px solid var(--qz-bd);border-radius:10px;padding:.05rem .45rem;}
.qz-tag.multi{color:var(--qz-p);border-color:var(--qz-p);font-weight:700;}
.qz-stem{font-weight:600;margin:0 0 .6rem;line-height:1.45;}
.qz-opt{display:flex;gap:.55rem;align-items:flex-start;padding:.34rem .5rem;border-radius:6px;
    cursor:pointer;line-height:1.4;border:1px solid transparent;}
.qz-opt:hover{background:rgba(127,127,127,.09);}
.qz-opt input{margin-top:.22rem;flex:none;}
.qz-q.done .qz-opt{cursor:default;}
.qz-q.done .qz-opt.right{border-color:var(--qz-a);background:rgba(46,125,50,.11);}
.qz-q.done .qz-opt.wrong{border-color:var(--qz-b);background:rgba(198,40,40,.11);}
.qz-mark{font-weight:700;flex:none;width:1.1em;}
.qz-foot{display:flex;gap:.55rem;align-items:center;margin-top:.55rem;}
.qz-btn{font-size:.74rem;padding:.3rem .7rem;border:1px solid var(--qz-p);background:transparent;
    color:var(--qz-p);border-radius:5px;cursor:pointer;font-family:inherit;}
.qz-btn:disabled{opacity:.4;cursor:default;}
.qz-verdict{font-size:.78rem;font-weight:700;}
.qz-verdict.ok{color:var(--qz-a);} .qz-verdict.no{color:var(--qz-b);}
.qz-why{margin-top:.55rem;padding:.6rem .75rem;border-left:3px solid var(--qz-p);
    background:rgba(21,101,192,.07);font-size:.88rem;line-height:1.5;border-radius:0 5px 5px 0;}
.qz-why[hidden]{display:none;}
.qz code{font-size:.9em;}
</style>

<div class="qz" id="qz">
  <div class="qz-bar" id="qz-filters"></div>
  <div class="qz-bar">
    <button class="qz-act" id="qz-all">Check all</button>
    <button class="qz-act ghost" id="qz-reset">Reset</button>
    <span class="qz-score" id="qz-score">Not started</span>
  </div>
  <div id="qz-list"></div>
  <div class="qz-bar">
    <button class="qz-act" id="qz-all2">Check all</button>
    <button class="qz-act ghost" id="qz-reset2">Reset</button>
  </div>
</div>

<script>
(function () {
  var Q = [
{"m":"SA-01","t":"single","q":"SABSA W100 names three things that determine any architecture. Which set is it?","o":["People, process and technology","Goals, environment and technical capability","Cost, schedule and scope","Threats, vulnerabilities and assets"],"a":[1],"e":"SA-01, <em>The Enterprise, Complexity, and Whether You Need an Architecture at All</em>. W100 reduces its building analogy to three determining factors: the goals the architecture must serve, the environment it is built in, and the technical capability available to build and run it. Where capability dominates goals, the architecture fails to deliver what the business needs, which the module uses as the diagnostic for most of its later failure modes."},
{"m":"SA-01","t":"single","q":"A small, isolated project has one designer who can hold the whole design in their head. Under W100's test, does it need an architecture?","o":["Yes: every project needs a signed-off contextual layer before work starts","Yes, but only at the component layer","No: architecture becomes necessary when many designers must work as one design authority, or when a project must fit a wider set of projects over time","Only if the project handles personal information"],"a":[2],"e":"SA-01, <em>The Enterprise, Complexity, and Whether You Need an Architecture at All</em>. W100 is direct: a small, isolated project does not need an architecture because one designer can hold it. Architecture becomes necessary when many designers must act as one design authority or when projects must fit together over years; the tools are layering and modularisation."},
{"m":"SA-01","t":"multi","q":"Which statements reflect W100's guidance on where a Business Attributes Profile comes from and how attributes are chosen?","o":["Source data comes from direct interviews with operational business managers, not from the IT or security team","W100's attribute taxonomies are examples: include only attributes that apply, create new ones where there are gaps, decompose where the organisation needs finer granularity","Tick the whole taxonomy so that nothing is missed","Every selected attribute gets an organisation-specific definition, and the profile is not finished until a business manager claims each one"],"a":[0,1,3],"e":"SA-01, <em>Building the Business Attributes Profile with Stakeholders</em>. W100 (p. 5) locates the source data in direct interviews with operational business managers and warns that technologists often believe they already know the requirements. Its taxonomies are examples, not comprehensive or mandatory; selecting from the whole taxonomy rather than from transcripts is the failure mode the labs are marked against."},
{"m":"SA-01","t":"single","q":"SABSA practice attaches two thresholds to every attribute metric. Which description is correct?","o":["A minimum and a maximum of technical performance, both set by the architect","A KPI threshold owned by the architect and a KRI threshold owned by the SOC","One threshold, with the other derived from the standard deviation","A primary threshold that is the limit of acceptable performance and therefore expresses risk appetite, plus an early-warning threshold that allows the risk to be steered back before appetite is breached"],"a":[3],"e":"SA-01, <em>Attribute Metrics and Thresholds as Architecture Artefacts</em>. W100 observes that failing a performance goal is a risk event, so a KPI read the other way is a KRI. SABSA practice sets a primary threshold (the limit of acceptable performance, hence risk appetite) and a secondary early-warning threshold, mapping onto green, amber and red reporting. The owner is a business manager, because the primary threshold is their appetite, not the architect's."},
{"m":"SA-01","t":"single","q":"Which of these is a properly formed enablement objective, as the module defines one?","o":["“Deploy multi-factor authentication for all account customers”","“Enable self-service booking for account customers by Q3, with Authenticated and Available holding at their primary thresholds from launch”","“Reduce the number of security incidents year on year”","“Achieve Essential Eight Maturity Level Two”"],"a":[1],"e":"SA-01, <em>Control Objectives, Enablement Objectives and the Opportunity Side of Risk</em>. A usable enablement objective names the opportunity in the sponsor's words, the attributes that must hold for the business to take it safely, and the metric that shows whether it was taken. “Deploy multi-factor authentication” is a mechanism and belongs three layers down; the other two options name neither an opportunity nor an attribute."},
{"m":"SA-01","t":"multi","q":"Which statements about the traceability register and its two sweeps are correct?","o":["The backward sweep starts at a component and asks why it exists; if the walk reaches no driver, the component is an orphan and W100's business-justification test has failed","The forward sweep starts at an attribute and walks down; an attribute with no strategy, or a strategy with no service, is a completeness gap","The architecture diagram, not the register, is what an auditor or assessor treats as evidence","W100 says the existence of such an architecture is one of the ways auditors establish that security is applied systematically"],"a":[0,1,3],"e":"SA-01, <em>The Traceability Chain as a Working Register</em>. The register earns its keep through the backward sweep (business justification) and the forward sweep (completeness). W100 adds that such an architecture is one way auditors establish security is applied systematically, which is why the register, not the diagram, is what audit and assurance treat as evidence. ASD's question about traceability back to business objectives is answered by the register."},
{"m":"SA-01","t":"single","q":"In the SABSA lifecycle as W100 describes it, where is the sign-off gate, the natural break in the development process?","o":["Once the contextual and conceptual architectures are agreed and signed off","Once the logical architecture is complete","Between Implement and Manage and Measure","Once the component layer has been priced"],"a":[0],"e":"SA-01, <em>The Lifecycle as an Operating Rhythm</em>. W100 identifies a natural break once the contextual and conceptual layers are agreed and signed off; before it, useful progress is difficult and going straight to products and tools is the source of many severe operational problems; after it, later phases can proceed with considerable parallel working. The module's suggested calendar (annual contextual review, quarterly measure, a board sitting per project) is its own synthesis, not W100, which gives no cadence."},
{"m":"SA-01","t":"single","q":"How does W100 position the service-management view relative to the five layers from contextual to component?","o":["Only as a sixth horizontal layer beneath the component layer","As a replacement for the physical layer","Vertically across the other five layers, because service-management questions arise at, and have meaning in, every layer","At the component layer only, where the tools live"],"a":[2],"e":"SA-01, <em>The Service-Management Layer as a Vertical Working Tool</em>. W100's Table 1 adds the service manager's view, but its Figure 1 gives what the paper calls the more helpful picture: the layer runs vertically across the other five. Two consequences: Supportable and Maintainable belong in the profile with thresholds, and the monitoring architecture is designed at the conceptual layer. The ITIL v3 alignment W100 states is dated and must not be cited as current."},
{"m":"SA-01","t":"single","q":"A project submission reaches the Architecture Board with an identity product already selected and no traceability to any attribute. Under the module's board decision flow, what should the board do first?","o":["Record an exception with an owner, expiry and compensating control and let it proceed","Return it: complete the trace to a signed-off attribute, or state the new driver the profile missed","Re-open the architecture at the component layer","Approve it, since product selection is the tradesman's accountability, not the board's"],"a":[1],"e":"SA-01, <em>Running the Practice: Blueprint, Road-map, Board and Maintenance</em>. The first question in the board's flow is whether every design decision is traceable to a signed-off attribute; if not, the submission is returned. An exception is for a project-specific inconsistency with the blueprint; re-opening is for a changed context. “Straight to products” is one of W100's named failure modes."},
{"m":"SA-01","t":"multi","q":"Which statements about accountability, per W100's view-to-layer mapping and the module's practice synthesis, are correct?","o":["The architect is accountable for the conceptual layer (profile, objectives, strategies, the traceability chain, blueprint and road-map) and not for logical design detail, product selection or operations","The service manager is accountable for setting the attribute thresholds","The designer produces the logical architecture, which must reflect every major strategy from the conceptual layer","W100 treats the Inspector (audit) and the Governor as two further separate layers of the model"],"a":[0,2],"e":"SA-01, <em>Accountability: What the Architect Owns</em>. W100's view-to-layer mapping is the accountability map: “The architect is an artist and visionary, but the designer is an engineer.” Thresholds are set by the business, not the service manager. W100 names the Inspector and the Governor as views it does not treat as separate layers; both consume the architect's register."},
{"m":"SA-02","t":"single","q":"Which of the following is <em>not</em> one of the three cornerstones W117 rests on?","o":["Risk management drives the selection of security measures","Requirements management is central, with Business Attribute Profiling as the technique TOGAF lacks","A notation such as ArchiMate is mandatory for security views","The ADM is the delivery process"],"a":[2],"e":"SA-02, <em>Why Integrate: Three Cornerstones and One Corollary</em>. W117's three cornerstones (p. 16) are that risk management drives the selection of security measures, that requirements management is central, and that the ADM is the delivery process. The corollary is that the security architect must be a member of the enterprise architecture team, not a reviewer consulted at the end. The module explicitly does not teach ArchiMate or any notation."},
{"m":"SA-02","t":"single","q":"W117 sets three mapping rules for placing SABSA artefacts in the ADM. Which option states them correctly?","o":["Map at the highest (enterprise) level where an artefact could appear at several; choose the most obvious of two defensible mappings; limit the integration to the most important elements","Map every artefact to every phase in which it could conceivably appear","Map at the lowest level of detail so that nothing is lost","Defer all mapping decisions to Phase H governance"],"a":[0],"e":"SA-02, <em>Why Integrate: Three Cornerstones and One Corollary</em>. The three mapping rules (W117 p. 16) explain why some artefacts sit in Phase G at enterprise level but in Phase C in a solution architecture, and why the module's artefact-by-phase table is an interpretation of the per-phase prose to be checked against W117's figures."},
{"m":"SA-02","t":"multi","q":"Which of these does W117 place in the Preliminary Phase?","o":["Business Drivers for Security","Risk Appetite","Security Audit","Security Resource Plan"],"a":[0,1,3],"e":"SA-02, <em>The Preliminary Phase: Security Context and the Security Resource Plan</em>. W117 sets five artefacts here: Business Drivers for Security, Security Principles, Key Risk Areas, Risk Appetite and the Security Resource Plan. Security Audit is a Phase G artefact. The Resource Plan is also where W117 states that not every artefact may be necessary and that delivering all of them does not guarantee security is properly addressed."},
{"m":"SA-02","t":"single","q":"Where in the TOGAF content metamodel does W117 propose the Business Attribute Profile should live?","o":["The Architecture Repository's Reference Library","The Motivation Extension, at the object Goal, adding measurement approach, specific metric(s) and performance target to Goal's existing attributes","A new Security Extension with its own objects","The Requirements Impact Assessment template"],"a":[1],"e":"SA-02, <em>From Business Attribute Profile to Requirements Catalog</em>. W117 (pp. 26–27) proposes the Motivation Extension at Goal, which already relates to Driver and Objective; Goal's six attributes (ID, Name, Description, Category, Source, Owner) gain measurement approach, specific metric(s) and performance target. TOGAF Architecture Repository partitions are not in W117 and the module omits them."},
{"m":"SA-02","t":"single","q":"In which ADM phase does an access-control policy belong, according to W117's own example?","o":["Always Phase B, because policy is business-level","Always the Preliminary Phase","Phase D at every level","Phase C in a solution architecture, but too detailed for an enterprise architecture, where it is left to Phase G"],"a":[3],"e":"SA-02, <em>Scope, Abstraction and Nested Architectures</em>. W117's own example (p. 30): the same policy is a Phase C artefact in a solution architecture but is left to Phase G at enterprise level. Where a project mixes levels, W117 rules that the enterprise level wins (p. 32), and it states that TOGAF is not equipped for solution architectures."},
{"m":"SA-02","t":"single","q":"Which statement reflects W117's position on security artefacts in Phase D, Technology Architecture?","o":["Usually no separate security artefact is needed, provided the Technology Architecture incorporates the controls defined earlier; the security architect verifies they are present and used effectively, and Security Standards is the Phase D artefact","Phase D is where the Security Domain Model is first produced","Phase D produces the Business Risk Model","Every engagement requires a dedicated technology security view in Phase D"],"a":[0],"e":"SA-02, <em>Phases B to D: Business-Level Security and the Catalogues</em>. The Security Domain Model and Business Risk Model are Phase B artefacts. W117's position on Phase D (p. 42) is that specific technology security artefacts are unnecessary if the Technology Architecture incorporates the controls defined earlier; Security Standards is the Phase D artefact, and a stakeholder <em>may</em> request a technology security view."},
{"m":"SA-02","t":"multi","q":"Which of these does W117 name as Phase G, Implementation Governance, artefacts?","o":["Security Management: detailed roles and responsibilities, security governance, KPIs and KRIs","Security Architecture Governance: the decision between a minor change and a new iteration","Security Audit: reviews of processes, designs and code, functional security testing and penetration testing","Security Awareness: training for deployment and operation, and awareness for all users"],"a":[0,2,3],"e":"SA-02, <em>Governance Touchpoints from the Security Seat</em>. Phase G carries Security Management, Security Audit and Security Awareness (W117 pp. 44–45). Phase H produces no tangible artefact but defines two essential processes, Risk Management and Security Architecture Governance; the latter decides between a minor change within the iteration and a new one. For ISM-scoped systems, Phase G is where security-assessment evidence is generated."},
{"m":"SA-02","t":"single","q":"What does W117 say about which assets are primarily at risk?","o":["Information assets are primary; business capabilities are derived from them","Business capabilities are the primary assets at risk; information and IT assets are secondary assets supporting them, which inverts the ISO/IEC 27005-style classification","Only IT assets can be assets at risk","Primary assets are whatever appears on the balance sheet"],"a":[1],"e":"SA-02, <em>Risk-Driven versus Compliance-Driven Enterprise Security</em>. W117 (pp. 19–20) makes business capabilities the primary assets at risk and information and IT assets secondary. Effective architecture means every driver is supported by a security service; efficient means every service is justified by a driver at minimal cost. “Compliance-driven” is the module's label, not W117's."},
{"m":"SA-02","t":"single","q":"A board-approved change in the organisation's risk appetite arrives at the change board of an existing ADM-based architecture. Under the module's decision flow, which iteration scope applies?","o":["Apply it within the current iteration through a Requirements Impact Assessment","Run a lightweight security iteration on the affected domain only","Run a full ADM iteration from the Preliminary Phase onward, because appetite is set there and W117 says a change in enterprise risk appetite can trigger a full cycle","Take no action until the next scheduled Phase H review"],"a":[2],"e":"SA-02, <em>When a Full ADM Iteration Is Not Warranted</em>. The decision flow is the module's guidance built on W117 statements: a change in enterprise risk appetite, or a seemingly small requirement change, can trigger a full cycle (pp. 46–47), whereas a change in security standards or requirements can usually be applied within the existing architecture. The trade-off is business improvement against threat against the disruption and cost of the change itself."},
{"m":"SA-02","t":"multi","q":"Which statements correctly describe what SA-02's sources do and do not support?","o":["ISM-2118, as cited from the September 2026 extract, expects vulnerability assessment and penetration testing before deployment of a significant change, which the module treats as the ISM-side counterpart to the Phase H decision","The ISM assurance extract read for the module sets out the system-authorisation process","W117 itself uses the TOGAF-general terms Architecture Board, architecture contract and dispensation","W117's glossary defines business imperatives as government and legal obligations an agency must meet even where its strategy documents are silent"],"a":[0,3],"e":"SA-02, <em>The Preliminary Phase: Security Context and the Security Resource Plan</em> and <em>Governance Touchpoints from the Security Seat</em>, with the module's Australian context section. ISM-2118 and the business-imperatives definition are sourced; the authorisation process is not in any source read and is deferred to SA-06; the TOGAF-general governance terms are labels the module uses for W117's touchpoints and are unverified against the current Standard."},
{"m":"SA-03","t":"single","q":"How does ASD's <em>Investing in modern defensible architecture</em> state MDA's relationship to the ISM and the Essential Eight?","o":["MDA replaces the Essential Eight Maturity Model for organisations at Maturity Level Three","ASD maps each Foundation to a set of ISM control identifiers","MDA is assessed through IRAP in place of the ISM","MDA complements and strengthens the Essential Eight Maturity Model and the ISM, and organisations should harden and protect existing systems in parallel; no control identifiers, maturity-level mapping or crosswalk is given"],"a":[3],"e":"SA-03, <em>What MDA Is, and What It Is Not</em>. That sentence is the entire stated relationship, and the module invents nothing beyond it; any MDA-to-ISM control or Essential Eight maturity mapping is deliberately omitted as unsupported by the source. ASD also states that MDA “cannot be bought off the shelf as a single product or specific toolset.”"},
{"m":"SA-03","t":"multi","q":"Which statements about ASD's three-stage MDA investment roadmap are correct?","o":["Stage 1 maps organisational strategy to MDA Foundations: a generational approach, business and security objectives, threats and risks, prioritised Foundations","Stage 2 identifies the people, skills and training required","Stage 3 assesses technology: stocktake, lifecycles, new technology requirements and a sustainment plan","The three stages are strictly sequential and independent, and only Stage 1 feeds the roadmap"],"a":[0,1,2],"e":"SA-03, <em>The Investment Roadmap and the Generational Approach</em>. The stages are presented in order but are interlinked; all three are necessary and each must feed the roadmap. ASD's question sets are non-exhaustive, and each new Foundation gives a positive feedback loop to those already in place, which is why the order of a roadmap is itself a design decision."},
{"m":"SA-03","t":"single","q":"Which MDA Foundation names does the module treat as verifiable from the source it read?","o":["All ten Foundations, with names and numbers","F2 high-confidence authentication, F3 contextual authorisation, F4 reliable asset inventory and F5 secure endpoint; “reduce attack surface” inferred as F6 and provisional; F1 and F7 to F10 not named","None: the source names no Foundations at all","F1 to F5 only"],"a":[1],"e":"SA-03, <em>What MDA Is, and What It Is Not</em>. The investment guidance names only F2 to F5 in its worked example and labels “reduce attack surface” as Foundation 6 in one table; the rest are never named. The module marks every other Foundation reference provisional and requires confirmation against ASD's companion <em>Foundations for modern defensible architecture</em> before relying on any name."},
{"m":"SA-03","t":"single","q":"In the module's initiative register, the asset-and-data-flow inventory initiative treats lower-rated threats than the authentication initiative yet is sequenced first. Why?","o":["Because inventory is the cheapest initiative in the register","Because ASD mandates asset inventory as Foundation 1","Because ASD's rule favours leveraging and enhancing existing technology and reducing attack surface first, and the Foundation feedback loop means the authentication and authorisation initiatives depend on inventory data","Because “worst risk first” is ASD's rule and inventory carries the worst residual risk"],"a":[2],"e":"SA-03, <em>Threat-Informed Prioritisation and the Initiative Register</em>. ASD's prioritisation is driven by business opportunities and threats, weighed against technology availability and compatibility, workforce skills, resources, cost, value and outcomes; initial investment leverages existing technology and reduces attack surface. The register is the artefact a funding decision is made on, and the sequence is not “worst risk first”."},
{"m":"SA-03","t":"single","q":"Under the module's method for reading CSF 2.0 Subcategories, what makes an outcome <em>structural</em>?","o":["It constrains how a system is composed, connected or placed, so it must be satisfied by the architecture and is expensive to retrofit","It constrains what people do and can usually be met by process on any architecture","It sits in the Govern function","It has an Implementation Example"],"a":[0],"e":"SA-03, <em>Deriving Architecturally Significant Requirements from CSF 2.0 Outcomes</em>. The structural-versus-procedural classification is the module author's method, not NIST's. NIST's own neutrality (outcomes are not a checklist; order and size imply neither sequence nor importance) is what makes the Core usable as a requirements source. An ASR without a testable fit criterion is not an ASR."},
{"m":"SA-03","t":"single","q":"A requirements register copies a CSF 2.0 Implementation Example word for word as a requirement. What does the module say about this?","o":["Correct practice: Implementation Examples are the required baseline","Acceptable, provided the matching Informative Reference is copied too","It has confused an illustration with an obligation: NIST states that Examples are notional, not comprehensive and not a baseline of required actions","Acceptable, because the CSF Core is a checklist of actions"],"a":[2],"e":"SA-03, <em>Deriving Architecturally Significant Requirements from CSF 2.0 Outcomes</em>. NIST states that Informative References can be narrower or broader than a Subcategory and that Implementation Examples are notional, not comprehensive and not a baseline of required actions. Use both as design hints and control candidates; the planted-Example error is the most common one in outcome-derived requirements registers."},
{"m":"SA-03","t":"single","q":"What does NIST CSWP 29 say about the scope of an Organizational Profile, and what makes it an architecture instrument in the module's reading?","o":["A Profile must always cover the whole enterprise","A Profile may cover the whole enterprise, a set of systems, or one threat against those systems; gap-closing actions provide key inputs into system-level plans, and a Target Profile can express expectations to suppliers and third parties","A Community Profile must be adopted before a Target Profile can be written","Profiles replace the ISM as an Australian organisation's assessed control baseline"],"a":[1],"e":"SA-03, <em>Organizational Profiles as Current and Target Architecture States</em>. An enterprise-scoped Profile is a governance artefact; a Profile scoped to one platform becomes that platform's requirements envelope, and its gap-closing action plan becomes the architecture backlog. NIST names a risk register, risk detail report or Plan of Action and Milestones as forms the action plan may take. In Australia the CSF supplies outcome vocabulary while the ISM supplies the controls."},
{"m":"SA-03","t":"single","q":"How does the module use CSF 2.0 Tiers?","o":["As a maturity score for the enterprise","As a substitute for an Essential Eight maturity level","As a control checklist per Tier","Narrowly: NIST's statement that progression to higher Tiers is encouraged when risks or mandates are greater, or when a cost-benefit analysis shows a feasible, cost-effective reduction of risk, used to justify investment for one platform"],"a":[3],"e":"SA-03, <em>Tiers as Investment Justification, Not Maturity Scoring</em>. The module uses Tiers for one purpose: the sentence NIST supplies about when progression is encouraged, turned into a business-case statement of mandate, Current Profile Tier, cost of Target Profile actions and risk reduction. Scoring the enterprise's maturity belongs to the governance units, not this module."},
{"m":"SA-03","t":"multi","q":"Applying the lens “control gap, not technology age”, which of the module's lifecycle-table rows are classified correctly?","o":["Identity provider, fully supported, only password and SMS factors enabled: control gap; enable phishing-resistant factors, no replacement needed","Legacy system with vendor support ended and last patch 30 months ago: age; isolate to enumerated flows and retire on a dated milestone","API gateway, supported, static keys because no one built the token flow: age; replace the gateway","Jump host with unsupported OS and no session recording: both; replace with a brokered, recorded access path"],"a":[0,1,3],"e":"SA-03, <em>Technology Stocktake, Lifecycles, Procurement and Sustainment</em>. ASD's example table carries the lens “control gap, not technology age”: asking only how old something is replaces supported technology that is merely misconfigured and keeps unsupported technology that is quietly critical. The API gateway is a control gap: configure short-lived credentials and add a training line item."},
{"m":"SA-03","t":"single","q":"In the measurement chain, how does the module distinguish KPIs from KRIs, and why must both reach the board?","o":["KPIs show progress toward a Target Profile outcome; KRIs show exposure that remains while the outcome is unmet; a board that sees only KPIs will fund the roadmap once and then stop","KPIs are technical and KRIs are financial","KRIs are reported only after an incident","KPIs replace KRIs once the Target Profile is reached"],"a":[0],"e":"SA-03, <em>Measuring Architecture Investment Against CSF Outcomes</em>. Three practice points: choose indicators the stocktake already measured so the baseline is real, distinguish KPIs from KRIs, and repeat the Profile cycle when a target is reached. Every row of the chain must be readable from either end, by a board member and by an engineer."},
{"m":"SA-04","t":"single","q":"In the ISM's sense, as the module reads the gateways chapter, what is a gateway?","o":["Any firewall","A router at the internet edge only","Whatever securely manages flows between networks in different security domains; it takes on the highest sensitivity or classification of the domains it joins","A device separating two VLANs within one security domain"],"a":[2],"e":"SA-04, <em>Security domains and the boundary decision ladder</em>. That definition, from the gateways chapter's context, reduces almost every structural decision to two questions about a pair of networks: are they in the same security domain, and what is the highest classification on either side?"},
{"m":"SA-04","t":"single","q":"Two networks sit in different security domains; neither is SECRET or TOP SECRET, and neither is public network infrastructure. Which pattern does the ladder give?","o":["VLAN segmentation, zoned by criticality","A gateway with an evaluated firewall; VLANs are never the separation mechanism between domains","A cross domain solution, with ASD consulted","A DMZ alone"],"a":[1],"e":"SA-04, <em>Security domains and the boundary decision ladder</em>. VLANs or MPLS are acceptable only within a security domain; a DMZ is added wherever external parties consume services; a cross domain solution is required when at least one side is SECRET or TOP SECRET. The identifiers the module attaches to each rung come from the September 2026 extracts and must be re-verified against the live ISM before use."},
{"m":"SA-04","t":"multi","q":"An organisation risk-manages VLANs between different security domains at the same classification. Which conditions does the networking chapter attach?","o":["The switches carrying them are administered from the highest-trust domain they serve","Meeting the conditions makes this the recommended pattern rather than an exception","Trunks are not shared across domains","Each domain's VLANs land on separate physical interfaces"],"a":[0,2,3],"e":"SA-04, <em>Security domains and the boundary decision ladder</em>. The networking chapter attaches those three device-level conditions where VLANs between different domains at the same classification are risk-managed. The module's verdict: a tolerated exception, not a pattern."},
{"m":"SA-04","t":"single","q":"Where does the ISM require the NIDS or NIPS to sit, according to the module?","o":["Outside the outermost firewall, so it sees all traffic","Inside the DMZ only","On the dedicated management network","In every gateway to networks the organisation does not manage, immediately inside the outermost firewall, alerting on any traffic a firewall rule should have stopped"],"a":[3],"e":"SA-04, <em>Gateway anatomy and control placement</em>. The module calls this the most precise placement rule in either chapter: with the sensor immediately inside the outermost firewall, a failed or misconfigured firewall is detected rather than assumed. A NIDS outside the outermost firewall is one of the planted errors in the module's placement-error exercise."},
{"m":"SA-04","t":"multi","q":"Which statements about the management plane does the module derive from the ISM chapters?","o":["Management interfaces of servers and network devices are reachable only from a dedicated management network that neither the general network nor the internet can reach","Gateway management traffic never traverses a network the gateway connects","A sole administrator is acceptable for a gateway between two domains provided they are formally trained","A component shared between two domains is administered by the higher domain's administrators or a mutually agreed third party"],"a":[0,1,3],"e":"SA-04, <em>The management plane</em>. Administrators of a two-domain gateway hold minimum privileges, work under separation of duties so no one administrator can abuse the gateway alone, and are formally trained; screening and clearance scale with the gateway's sensitivity. A design with a sole administrator has already failed the chapter. The authentication-server tier (RADIUS wrapped in IPsec or TLS) is part of the management plane."},
{"m":"SA-04","t":"single","q":"In the content-filtering pipeline, a file arrives whose type is not on the allow-list. What happens to it?","o":["Block","Quarantine pending review","Convert the file type, strip active content and deliver","Deliver, with the event logged"],"a":[0],"e":"SA-04, <em>The content-filtering pipeline</em>. The pipeline has three exits: deliver, quarantine, block. A file type not on the allow-list is blocked before validation; malicious or uninspectable content is blocked; suspicious content is quarantined pending review; clean content is converted, stripped of active content, delivered and logged. The design decisions are which steps run in parallel, where the quarantine store sits (reachable by reviewers, not by the untrusted side), and what happens when a step fails open."},
{"m":"SA-04","t":"single","q":"Do firewalls, 802.1X components and certificate authorities need a high assurance evaluation at SECRET and TOP SECRET? What does the module say?","o":["Yes, in every case","Not determined from the sources read: it is decided in the ISM's evaluated-product guidelines, so the module asserts neither position","No: the evaluated tier is sufficient at every classification","Only for firewalls"],"a":[1],"e":"SA-04, <em>Cross domain solutions and assurance tiers</em>. The gateways chapter does require high assurance evaluation for diodes on one-way paths leaving SECRET or TOP SECRET networks and for peripheral switches between such systems and any other; for firewalls, 802.1X components and certificate authorities the question is decided in the ISM's evaluated-product guidelines, which were not a source, so the module leaves it undetermined rather than guessing."},
{"m":"SA-04","t":"single","q":"What distinguishes a Transfer CDS from an Access CDS?","o":["A Transfer CDS is unidirectional only; an Access CDS is bidirectional","An Access CDS is a form of VPN","A Transfer CDS moves data one way or both ways between domains; an Access CDS lets a user reach several domains from one device without moving data between them","A Transfer CDS is always a single diode appliance"],"a":[2],"e":"SA-04, <em>Cross domain solutions and assurance tiers</em>. A CDS is a system of security-enforcing functions built for the specific risks of accessing or moving data between domains, more often discrete sub-systems than one appliance. The CDS section extends the gateways section, so every gateway expectation still applies, with ASD consulted, upward and downward paths isolated, a quarterly sampled review of transfer-policy events, and the filter tested to prove it cannot be bypassed."},
{"m":"SA-04","t":"multi","q":"Which wireless measures does the ISM, as the module reads it, say do <em>not</em> help?","o":["Hiding the SSID, which enables evil-twin credential theft","MAC address filtering","WPA3-Enterprise 192-bit mode","Static IP addressing"],"a":[0,1,3],"e":"SA-04, <em>Network access control and the link layer</em>. The module lists three things the ISM says do not help: hiding the SSID (evil-twin credential theft), MAC filtering and static addressing. WPA3-Enterprise 192-bit mode is what protects wireless traffic; admission is 802.1X with EAP-TLS and every other EAP method disabled, both devices and users hold certificates, guest wireless is segregated, and access-point administration is unavailable over the wireless interface."},
{"m":"SA-04","t":"single","q":"Under NIST SP 800-63-4's initial selection, a user group assessed at High effective impact maps to which levels?","o":["IAL2, AAL2 and FAL2","IAL3, AAL3 and FAL2 or FAL3, with the FAL choice turning on whether a compromised identity provider would sit inside or outside the enterprise boundary","IAL1, AAL1 and FAL1","AAL3 only; IAL and FAL are not selected per user group"],"a":[1],"e":"SA-04, <em>IAL, AAL and FAL as boundary requirements</em>. SP 800-63-4 (Sec. 3.3.3) maps Low to the level-1 xALs, Moderate to level 2, and High to IAL3, AAL3 and FAL2 or FAL3, with the FAL decision the document's one link between placement and assurance. Its SHALL statements bind US agencies; in Australia it is a shared vocabulary only, and the module's worked ledger is provisional. Machine-to-machine, IoT and API-on-behalf boundaries fall outside its scope and need ISM-derived assurance statements instead."},
{"m":"SA-05","t":"single","q":"What are the three tiers of the NIST SP 800-92 log management infrastructure model?","o":["Log generation; log analysis and storage; log monitoring","Collection; correlation; response","Endpoint; network; cloud","Hot; warm; cold"],"a":[0],"e":"SA-05, <em>The Three-Tier Reference Model</em>. NIST SP 800-92 frames the problem as balancing a limited quantity of log management resources with a continuous supply of log data, and structures the answer as generation, analysis and storage, and monitoring. Placement decisions live in the second tier, which NIST describes in four shapes."},
{"m":"SA-05","t":"single","q":"An estate has six regional offices on unreliable WAN links and a central logging tier that must not be directly reachable from those sites. Which NIST second-tier shape fits?","o":["A single server","Specialised servers: collect and analyse on one, store long term on another","Multiple servers with failover, each serving a subset of generators","Two levels: first-level servers forward to a central level"],"a":[3],"e":"SA-05, <em>The Three-Tier Reference Model</em>. NIST's two-level shape is the one for multiple sites, unreliable or Internet-only links, or a central tier that must not be directly reachable: first-level caching servers buffer when the link is down and shield the central tier from direct attack. Its cost is two hops of latency and first-level caches that must be sized and protected. It is the ancestor of every relay design in the module."},
{"m":"SA-05","t":"single","q":"Why does ASD warn against treating the SIEM as the repository for every log?","o":["Because a SIEM cannot retain more than 90 days of data","Because the centralised facility is what the Essential Eight requires instead","Because law and regulation will require many sources to be logged, but the SIEM should hold only the security logs the organisation's risk profile justifies, and most SIEM pricing is keyed to ingested volume","Because logs held in a SIEM cannot be used as evidence"],"a":[2],"e":"SA-05, <em>Centralised Facility, Then SIEM</em>. ASD's two-stage premise: centralise first, then ingest a risk-justified subset into the SIEM. The centralised facility's failure mode is loss of evidence; the SIEM's is loss of detection. Small organisations may collapse the two into one platform, provided the SIEM's ingest is a chosen subset rather than an accident of history."},
{"m":"SA-05","t":"multi","q":"According to ASD's <em>Best practices for event logging and threat detection</em>, which items belong in an enterprise-approved event logging policy?","o":["Which events are logged and which facilities are used","How logs are monitored and how long they are kept","When to reassess what is worth collecting, and responsibilities shared with service providers","The vendor and model of every collector"],"a":[0,1,2],"e":"SA-05, <em>The Logging Policy as the Governing Artefact</em>. Those are the contents ASD gives for an enterprise-approved event logging policy, its first key factor. NIST adds the mandatory-versus-recommended split across generation, transmission, storage and disposal, and analysis, and warns that enabling every auditable event can cripple a host. Legal and privacy review is a policy input, not a post-deployment check."},
{"m":"SA-05","t":"single","q":"The ASD sources supply two orderings for selecting log sources. Which option describes them correctly?","o":["Both order strictly by expected volume","<em>Best practices</em> orders by asset criticality and <em>Priority logs</em> by source category; neither is binding, and a per-source assessment precedes onboarding","<em>Priority logs</em> mandates endpoint detection and response first for every organisation, regardless of assessment","CREST's 2015 source tiering is the binding order"],"a":[1],"e":"SA-05, <em>Selecting Sources</em>. The per-source assessment records purpose, priority, volume, analytical value, reliability, generation precondition and known blind spots. Generation is part of the architecture (many channels exist only once an audit setting enables them), value and noise are separate axes, and onboarding is incremental with regular health checks on high-priority sources. CREST's source tiering is dated and not reused."},
{"m":"SA-05","t":"multi","q":"Which statements about collection and forwarding topology, generalised from ASD's <em>Windows event logging and forwarding</em>, are correct?","o":["Every collector class has a stated ceiling, collectors are placed by site and link capacity, and sources are routed to their nearest collector by policy rather than by hand","Subscriptions that drop noisy channels (ASD's example is Kerberos logon events) create blind spots that must be documented, with where the un-forwarded data remains","A new subscription by default reads each host's existing archive, producing a burst when hosts first forward","The collector ceiling of about 10,000 hosts and under 10,000 events per second is ASD's own measured figure"],"a":[0,1,2],"e":"SA-05, <em>Collection and Forwarding Topology</em>. WELF's rules generalise to any operating system and agent: ceilings, placement by site and link capacity, policy routing, subscription filtering with documented blind spots, and the initial burst. The ceiling figure is ASD citing Microsoft guidance; the module's verification table says not to present it as ASD's own figure. Rate limiting protects the server but discards the flooding source's messages during the very event that produced them."},
{"m":"SA-05","t":"single","q":"What does ASD's <em>Best practices</em> say about time in a logging architecture?","o":["Local time zone at each site, converted at the SIEM","Two-way synchronisation between OT and IT so both stay aligned","Time distributed from the SIEM to every source","Trustworthy time used consistently with multiple sources; UTC in ISO 8601 with milliseconds; one-way synchronisation, OT taking time from IT and never the reverse"],"a":[3],"e":"SA-05, <em>Function Placement, Integrity and Time</em>. Time is the correlation key, and CREST notes that organisations that believe their logs are synchronised often are not. The same section places normalisation at the point of centralisation, keeps original logs unaltered so copies are usable as evidence, and treats pipeline-health telemetry (audit-policy changes, log clearing, logging failures, forwarding errors) as a function in its own right."},
{"m":"SA-05","t":"single","q":"What central-store retention does the module take from the ASD sources, and on what reasoning?","o":["At least 18 months, which WELF attributes to <em>Strategies to Mitigate Cyber Security Incidents</em>, longer where regulation requires; <em>Best practices</em> reasons that discovery can take 18 months and malware dwells 70 to 200 days","90 days hot, nothing cold","Seven years for every source","Twelve months, fixed by NIST SP 800-92"],"a":[0],"e":"SA-05, <em>Retention as a Three-Layer Sizing Problem</em>. Each event has up to three copies: the source buffer (sized to outlive the longest forwarding outage), the collector archive, and the central store. NIST's 2006 impact-keyed values are not to be adopted; the reusable artefact is the shape, settings keyed to impact, re-keyed in Australia to the organisation's classification and obligations. Routine retention is distinguished from preservation, and dual-location storage is a design principle rather than redundancy."},
{"m":"SA-05","t":"multi","q":"Which log-flow options for operational technology does ASD's <em>Priority logs</em> name?","o":["A dedicated OT SIEM, which doubles the systems staff must know","ICS monitoring products that parse OT-native protocols and emit contextual logs","Full endpoint agents installed on every programmable controller","Data diodes moving logs from OT to the IT SIEM without exposing OT, where security supersedes other requirements"],"a":[0,1,3],"e":"SA-05, <em>Log Flow Across Trust Boundaries</em>. Most OT devices run constrained embedded software that heavy logging can degrade; where a device cannot log, or logs in non-standard formats, the network traffic around it is logged instead, and direct logging must be conservative and tested because of safety-critical deterministic messaging. <em>Priority logs</em> names the dedicated OT SIEM, ICS monitoring products and data diodes as the flow options."},
{"m":"SA-05","t":"multi","q":"Which of these are executive decisions that ASD's <em>Implementing SIEM and SOAR platforms: Executive guidance</em> names, as the module presents them?","o":["Sensitive or critical-service organisations may need to keep monitoring in-house; a provider is assessed for round-the-clock cover, security posture and foreign storage, and the contract fixes how compliance is verified, what visibility is returned and how liability divides","Deploy SOAR before the SIEM to reduce analyst load from day one","Examine hidden costs, especially ingest-based pricing and caps, and plan for sustained costs such as training","Get the SIEM alerting accurately before adding SOAR, and test alerting internally, then externally once mature"],"a":[0,2,3],"e":"SA-05, <em>Structuring the Capability and Choosing Where It Lives</em> and <em>Capacity, Reference Topologies and Assuring the Architecture</em>. ASD's <em>Executive guidance</em> names the sourcing decision, hidden and sustained costs, SIEM-before-SOAR sequencing and internal-then-external testing as the executive decisions a monitoring capability requires. The <em>Splunk Validated Architectures</em> example is cited once, for its threshold-driven method rather than the product."},
{"m":"SA-06","t":"single","q":"What does the ISM <em>Guidelines for security assurance</em> chapter, as read for the module, contain and delegate?","o":["A full authorisation-to-operate process with a named authorising officer","Two sections, security monitoring and security assessments, with no authorisation controls; who must ensure an assessment happens is delegated to the roles chapter, and what an assessment needs to see to the documentation chapter","The Essential Eight assessment method","The accreditation rules for IRAP assessors"],"a":[1],"e":"SA-06, <em>What the assurance chapter covers, and what it delegates</em>. The chapter as read defines no authorising officer, authorisation decision, system security plan or residual-risk acceptance, so the module's authorisation material is written generically and marked provisional. It does state an independence expectation for whoever assesses, which means the architect must not score their own design."},
{"m":"SA-06","t":"single","q":"A collection tier delivers security logs to the central facility promptly, but nothing in it can report its own forwarding latency. How does the module judge it against the promptness control?","o":["It passes, because the assessor accepts the architect's word that the property holds","It passes if the SIEM vendor certifies its latency","It has failed the control in evidence even if it satisfies it in fact: the design must produce the artefact an assessor can check without taking the architect's word","Forwarding latency is not an assurance concern"],"a":[2],"e":"SA-06, <em>Control, design property, evidence: the collection tier</em>. Each control is read three times: for its intent, for the design property it imposes and for the artefact an assessor can check. The artefact column is mostly operational output, so the design has to produce it, and the evidence pack's mapping must record each control's revision and update date, not only its identifier."},
{"m":"SA-06","t":"multi","q":"Which statements about timeliness, hunting and retention in the chapter's security monitoring section are correct?","o":["Timeliness controls are set per asset class, and the Essential Eight column shows an escalating coverage expectation that the architect reads as a sizing input","Threat hunts informed by current strategic and sector-specific CTI are expected at least every three months, with findings documented, actioned and fed back into automated monitoring","The records-management regime can never require longer retention than the twelve-month security minimum","Logs must remain searchable for at least twelve months, and retention must also satisfy the class-based minima in the records-management schedule the chapter names (AFDA Express Version 2, as cited)"],"a":[0,1,3],"e":"SA-06, <em>Monitoring capability, timeliness classes, hunting and retention</em>. Two overlapping retention regimes apply: a twelve-month searchability minimum and the class-based minima of the records schedule the chapter cites as AFDA Express Version 2 (the module marks that title's currency provisional), which can be longer and vary by record class. The quarterly hunt generates recurring evidence and requires searchable history, query capability and a path from hunt output to detection content. CTI services and suitable AI models to augment detection are new expectations the chapter leaves undefined."},
{"m":"SA-06","t":"single","q":"What scanning cadence logic does the chapter's security assessments context give?","o":["Scan at roughly twice the frequency at which patches must be applied (the worked example is weekly scanning for a fortnightly patch window), from both internal and external perspectives, with assets discovered automatically","Scan monthly regardless of the patch window","Scan only after each patch is applied, to confirm it worked","Scan from the external perspective only, since that reflects exposure"],"a":[0],"e":"SA-06, <em>Continuous assurance: the cadences the ISM already fixes</em>. The chapter fixes minimum cadences per asset class (daily for online services and internet-facing operating systems, weekly for productivity suites, browsers, email clients, PDF applications and security products, fortnightly for the rest), an event-driven reassessment while a working exploit exists, and lifecycle gates for deeper testing. Read together they are the mechanism by which the architecture produces evidence continuously, provided the outputs are captured as dated artefacts."},
{"m":"SA-06","t":"single","q":"Which re-authorisation triggers can an architect write directly from the chapter, and what is their status?","o":["An annual authorisation-to-operate expiry that the chapter defines","The authorising officer and plan of action that the chapter names","Testing before deployment, before a significant change and at least every six months (ISM-2118 as cited), plus frequent reassessment of likelihood while a working exploit exists for an unmitigated vulnerability (ISM-1921 as cited); the authorisation decision and its terminology are not in the chapter and are marked provisional","None: the chapter treats authorisation as a one-off event"],"a":[2],"e":"SA-06, <em>Authorisation gates, re-authorisation and residual-risk acceptance</em>. The chapter fixes the timing but not the decision. The module adds two capability-side triggers: a material change in the threat profile (CTI-CMM RISK and SITUATION domains) and a change to the target capability state (SOCTOM). A residual-risk statement names dated threats, unmitigated findings with compensating controls, accepted risks with owner and expiry, the assumptions the acceptance depends on, and the triggers that reopen it."},
{"m":"SA-06","t":"multi","q":"Which statements about SOC-CMM scoring are correct?","o":["Business, People and Process are scored for maturity only; Technology and Services for maturity and capability","The maturity scale runs 0 to 5 and the capability scale 0 to 3; the model is continuous, so fractional interim targets are possible","The 2024 whitepaper warns that weighting can compromise objectivity and advises the basic version when in doubt","A screening produces scores comparable with a full assessment"],"a":[0,1,2],"e":"SA-06, <em>SOC-CMM: maturity, capability and what a score is worth</em>. A screening produces no scores; a scoped assessment re-validates only what changed. The 2025 paper defines maturity as a property of internal process and capability as a property of output, and reports self-assessed scores running markedly above third-party scores, which is why independence is a design rule and a result is citable only with scope, date, model version, assessor and weighting choice."},
{"m":"SA-06","t":"single","q":"Which option states the SOC target operating model (SOCTOM) method's first principle and the sequencing rule the module adopts?","o":["Set every domain's target at the top of the scale so the roadmap is unambiguous","Always fix the lowest-scoring domain first","Delete rows that do not apply so the tool stays clean","A maturity level is derived from the ambition, not chosen; sequencing follows risk, so a lower-scoring domain may be deprioritised when a higher-scoring one carries more direct risk"],"a":[3],"e":"SA-06, <em>Deriving the target state with the SOCTOM method</em>. The five steps run from ambition and success criteria through horizon, current state and a concrete target to senior approval. Rows that do not apply are marked with an explanation rather than deleted, because the result is more auditable. The 2024 whitepaper also warns that insufficient maturity anywhere eventually becomes a risk; its example is a missing sourcing process, invisible until analysts become scarce."},
{"m":"SA-06","t":"single","q":"A target profile proposes HMM4 hunting, CTI1 in THREAT, SOC-CMM Technology capability 1 and three months of searchable retention. What does the coherence check find?","o":["HMM4 hunting cannot sit on CTI1 intelligence; the automation HMM4 requires cannot sit on SOC-CMM Technology capability 1; and three months of searchable history is short of both the twelve-month searchability expectation the module cites and the searchable history that hunting needs","Nothing: the profile is coherent for a twelve-month horizon","Only the retention figure is wrong","HMM4 hunting requires CTI0, so the CTI target is set too high"],"a":[0],"e":"SA-06, <em>CTI-CMM and the Hunting Maturity Model as architecture requirements</em>. The module's coherence rule: HMM4 hunting cannot sit on CTI1 intelligence or HMM1 data collection, and CTI2 automation at scale cannot sit on Technology capability 1. HMM2, the recommended first target, already needs routine collection of many data types and searchable history; the module notes that no platform buys HMM4."},
{"m":"SA-06","t":"single","q":"At CTI2, what does CTI-CMM's ARCHITECTURE domain let the architecture function expect from the CTI programme?","o":["A mandated threat intelligence platform product","Established channels, regular reports on cross-cutting threats, and advance notice of changes to CTI infrastructure and their likely effect","Control-compliance evidence against the ISM","A complete threat model for every system in the estate"],"a":[1],"e":"SA-06, <em>CTI-CMM and the Hunting Maturity Model as architecture requirements</em>. CTI-CMM practices are cited as DOMAIN-Objective.Practice; at CTI2 the architecture function can expect ARCHITECTURE-1.b, 2.b and 3.c, and at CTI3 contextualised trend reporting on a regular cadence. The model calls itself descriptive, not prescriptive, so its levels are never presented to an assessor as control compliance."},
{"m":"SA-06","t":"multi","q":"Which statements about the evidence pack are correct?","o":["The evidence-pack structure is the module's own synthesis: none of the sources read defines an assurance evidence pack or a residual-risk statement as a named artefact","A maturity score is a claim about process and a control mapping a claim about design; only monitoring evidence and independent assessment results are claims about operation","CREST's incident-response maturity tool illustrates that a maturity score without its evidence column is a questionnaire","CTI-CMM recommends throughput metrics that show activity as the best measure of programme growth"],"a":[0,1,2],"e":"SA-06, <em>Assembling the evidence pack</em>. The pack's eight sections each answer one assessor question, and the limitations section is the one an assessor reads first. CTI-CMM warns that most organisations produce throughput metrics that show activity rather than growth; its written-decision practice is reused as the pack's decision log, and the structure is to be reconciled with the live ISM documentation chapter before use."}
  ];
  var list = document.getElementById('qz-list');
  var filter = 'all';

  function esc(s) { return s; }

  Q.forEach(function (q, i) {
    var d = document.createElement('div');
    d.className = 'qz-q';
    d.dataset.module = q.m;
    var opts = q.o.map(function (o, j) {
      var type = q.t === 'multi' ? 'checkbox' : 'radio';
      return '<label class="qz-opt" data-i="' + j + '">' +
             '<input type="' + type + '" name="q' + i + '" value="' + j + '">' +
             '<span class="qz-mark"></span><span>' + esc(o) + '</span></label>';
    }).join('');
    d.innerHTML =
      '<div class="qz-meta"><span class="qz-tag">' + q.m + '</span>' +
      (q.t === 'multi' ? '<span class="qz-tag multi">select all that apply</span>' : '') +
      '<span>Q' + (i + 1) + '</span></div>' +
      '<p class="qz-stem">' + esc(q.q) + '</p>' +
      '<div class="qz-opts">' + opts + '</div>' +
      '<div class="qz-foot"><button class="qz-btn" type="button" data-check="' + i + '">Check</button>' +
      '<span class="qz-verdict"></span></div>' +
      '<div class="qz-why" hidden><strong>Why:</strong> ' + esc(q.e) + '</div>';
    list.appendChild(d);
  });

  var cards = Array.prototype.slice.call(list.children);

  function chosen(i) {
    return Array.prototype.slice
      .call(cards[i].querySelectorAll('input:checked'))
      .map(function (el) { return parseInt(el.value, 10); })
      .sort(function (a, b) { return a - b; });
  }

  function check(i) {
    var q = Q[i], card = cards[i], picked = chosen(i);
    if (!picked.length) return false;
    var want = q.a.slice().sort(function (a, b) { return a - b; });
    var right = picked.length === want.length &&
                picked.every(function (v, k) { return v === want[k]; });
    card.classList.add('done');
    card.querySelectorAll('input').forEach(function (el) { el.disabled = true; });
    card.querySelectorAll('.qz-opt').forEach(function (lab) {
      var j = parseInt(lab.dataset.i, 10);
      var isAns = want.indexOf(j) !== -1, isPicked = picked.indexOf(j) !== -1;
      var mark = lab.querySelector('.qz-mark');
      if (isAns) { lab.classList.add('right'); mark.textContent = '✓'; mark.style.color = 'var(--qz-a)'; }
      else if (isPicked) { lab.classList.add('wrong'); mark.textContent = '✗'; mark.style.color = 'var(--qz-b)'; }
    });
    var v = card.querySelector('.qz-verdict');
    v.textContent = right ? 'Correct' : (picked.length && want.length > 1 ? 'Not quite' : 'Incorrect');
    v.className = 'qz-verdict ' + (right ? 'ok' : 'no');
    card.querySelector('.qz-why').hidden = false;
    card.querySelector('[data-check]').disabled = true;
    card.dataset.result = right ? 'right' : 'wrong';
    return true;
  }

  function score() {
    var done = cards.filter(function (c) { return c.dataset.result && !c.hidden; });
    var ok = done.filter(function (c) { return c.dataset.result === 'right'; }).length;
    var visible = cards.filter(function (c) { return !c.hidden; }).length;
    var el = document.getElementById('qz-score');
    el.textContent = done.length
      ? ok + ' / ' + done.length + ' correct  (' + done.length + ' of ' + visible + ' answered)'
      : 'Not started';
  }

  list.addEventListener('click', function (e) {
    var b = e.target.closest('[data-check]');
    if (!b) return;
    check(parseInt(b.dataset.check, 10));
    score();
  });

  function checkAll() {
    cards.forEach(function (c, i) { if (!c.hidden && !c.dataset.result) check(i); });
    score();
  }

  function reset() {
    cards.forEach(function (c) {
      c.classList.remove('done');
      delete c.dataset.result;
      c.querySelectorAll('input').forEach(function (el) { el.disabled = false; el.checked = false; });
      c.querySelectorAll('.qz-opt').forEach(function (lab) {
        lab.classList.remove('right', 'wrong');
        lab.querySelector('.qz-mark').textContent = '';
      });
      c.querySelector('.qz-verdict').textContent = '';
      c.querySelector('.qz-why').hidden = true;
      c.querySelector('[data-check]').disabled = false;
    });
    score();
    window.scrollTo({ top: document.getElementById('qz').offsetTop - 20, behavior: 'smooth' });
  }

  ['qz-all', 'qz-all2'].forEach(function (id) {
    document.getElementById(id).addEventListener('click', checkAll);
  });
  ['qz-reset', 'qz-reset2'].forEach(function (id) {
    document.getElementById(id).addEventListener('click', reset);
  });

  var mods = ['all'].concat(Q.map(function (q) { return q.m; })
    .filter(function (m, i, a) { return a.indexOf(m) === i; }).sort());
  var bar = document.getElementById('qz-filters');
  mods.forEach(function (m) {
    var b = document.createElement('button');
    b.className = 'qz-chip';
    b.type = 'button';
    b.textContent = m === 'all' ? 'All modules (' + Q.length + ')' : m;
    b.setAttribute('aria-pressed', m === 'all' ? 'true' : 'false');
    b.addEventListener('click', function () {
      filter = m;
      bar.querySelectorAll('.qz-chip').forEach(function (o) {
        o.setAttribute('aria-pressed', o === b ? 'true' : 'false');
      });
      cards.forEach(function (c) { c.hidden = (filter !== 'all' && c.dataset.module !== filter); });
      score();
    });
    bar.appendChild(b);
  });

  score();
})();
</script>
